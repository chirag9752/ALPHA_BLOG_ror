<h1>hello bhai from root</h1>

<%= link_to "Article_Listining", articles_path %>
<br/>
<br/>
<br/>

<% if logged_in? %>

<%= link_to current_user.username, user_path(current_user) %>

<p> <%= "(Admin)" if current_user.admin? %> Profile [ <%= current_user.username %>] </p>

<%= button_to 'Log out', logout_path, method: :delete %>

<%else%>

<%= link_to 'Log in', login_path %>

<%= link_to "Sign up", signup_path %>

<%end%>